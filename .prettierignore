dist/
node_modules/
.turbo/
coverage/
pnpm-lock.yaml

# Emitted protocol artifacts — emit-all output is canonical; verify-generated
# compares byte-for-byte, so external reformatting breaks the build.
packages/protocol/generated/

# Intentionally malformed fixtures for extraction tests.
packages/core/tests/extraction/__fixtures__/malformed.html

# Stale agent worktrees managed outside the repo.
.claude/
