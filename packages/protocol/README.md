# @yantra/protocol

Yantra Protocol — Zod schemas as the single source of truth for the agent/engine
contract. TypeScript types, JSON Schema, and pi-agent-core tool definitions are
regenerated from these schemas.

Populated by FEAT-002 (`feature_protocol_v0.md`). This package currently exports
only `PROTOCOL_VERSION` as a smoke artifact.
