<!-- Generated by spec-lite | skill: document-usage -->

# Usage

Yantra is a local-first command-line application for public-web research,
governed browser tasks, and deterministic saved workflows. This guide assumes
the source build described in the [quickstart](quickstart.md).

The current source workspace does not install a global binary. In the examples
below, replace `yantra` with `node apps/cli/dist/bin.js` when running from the
repository root. Use live help as the option source of truth:

```console
node apps/cli/dist/bin.js --help
node apps/cli/dist/bin.js ask --help
node apps/cli/dist/bin.js daemon start --help
```

## Choose an execution mode

| Goal                            | Command                   | Model behavior                                                                                                 |
| ------------------------------- | ------------------------- | -------------------------------------------------------------------------------------------------------------- |
| Answer one focused web question | `yantra ask <query>`      | Agentic when credentials are available; `--no-llm` is deterministic                                            |
| Investigate a broad topic       | `yantra research <topic>` | Agentic when credentials are available; `--no-llm` is bounded multi-hop research                               |
| Browse or act on a website      | `yantra do <goal>`        | Requires a model; no deterministic `do` executor exists                                                        |
| Replay a saved workflow         | `yantra run <name>`       | Steps are always deterministic; an optional model may only write the final Brief when the workflow declares it |

### Ask

```console
yantra ask "what changed in the EU AI Act?"
yantra ask "compare current residential solar incentives" --no-llm
yantra ask "latest USB-C charging rules" --format md --length long
yantra ask "current battery recycling requirements" --json
```

`--search-provider` selects `auto`, `google`, `duckduckgo`, `brave`, or
`tavily`. `--provider` selects the unrelated language-model provider. `auto`
walks the configured fallback chain and skips keyed providers whose key is
missing. Explicit Brave or Tavily selection requires its OS-keychain key.

Deterministic controls include `--limit`, `--max-sources`, `--fetch-timeout`,
`--pipeline-timeout`, `--no-cache`, `--detail`, `--length`, and
`--format terminal|md|html|json`. See [Ask](features/ask.md) for current bounds,
cache behavior, evidence semantics, and limitations.

### Research

```console
yantra research "state of grid-scale battery storage in 2026"
yantra research "long-duration storage technologies" --no-llm --depth 3
yantra research "right-to-repair laws" --no-llm --max-sources 12 --per-query-limit 4 --pipeline-timeout 90s
```

`--depth`, `--max-sources`, `--per-query-limit`, `--fetch-timeout`,
`--pipeline-timeout`, and `--max-llm-calls` govern only deterministic research.
Agentic research uses the shared duration, token, tool-timeout, and retry
budgets. Read [Research](features/research.md) for the two algorithms and their
different source/citation guarantees.

### Agentic browser tasks

The following forms require configured model access. Browser tasks also require
system Chrome and may contact the named sites:

```console
yantra do "compare the current return policies for these stores"
yantra do "complete the public demo form" --allow-host forms.example.com
yantra do 'sign in as @username{u1} with @password{p1}' --allow-host example.com
yantra do "look up the current order status" --save-as order-status
yantra do "check the public status page" --json
```

Repeat `--allow-host` to restrict a run to several user-approved hosts.
`@{value}` and tagged markers keep run-scoped values out of model-visible text
and persisted artifacts inside Yantra, but they do not hide process arguments or
shell history. Protected clicks and stored-secret fills ask for consent on an
interactive TTY and fail closed under `--json` or non-interactive execution.

`discover` is an alias for `do`. See
[Agentic Tasks](features/agentic-tasks.md) and
[Safety and Privacy](features/safety-and-privacy.md) before using browser
mutation or sensitive input.

## Saved workflows

List and inspect local workflows:

```console
yantra list workflows
yantra show order-status --workflow
yantra lint ./order-status.yaml
yantra lint ./order-status.yaml --strict --json
```

Replay one with parameters:

```console
yantra run order-status --params order_id=A123
yantra run order-status --params-file ./params.yaml
yantra run order-status --params-file ./params.yaml --params order_id=A123 --json
```

Explicit `--params` values override the parameter file. Parameters are not a
secret channel; credential-shaped values are rejected. Workflows live in
`<data-dir>/workflows/` and can be created by `do --save-as` or by editing YAML.
There is no public recorder command.

A workflow's optional `synthesis.use_llm` field controls whether a model may
write its final Brief. Model selection never changes replay steps, branching,
loops, or locators. `--no-llm`, scheduled execution, and nested workflow calls
force deterministic synthesis.

Resume is intentionally narrow:

```console
yantra resume <run-id>
yantra resume <run-id> --force
```

Only eligible failed or paused `cookies: auto` runs can resume. The checkpoint
does not restore prior captures, page state, named locators, or final synthesis;
rerun ordinary browser workflows from the beginning when the remaining suffix
is not self-contained. The shared agent budget flags appear in `run` and
`resume` help but do not currently limit replay execution. See
[Saved Workflows](features/saved-workflows.md).

## Report templates

Templates are local Markdown structures whose typed slots are filled by a
model. They are supported by `ask`, `research`, and `do`, not `run`.

```console
yantra template new weekly --tags work,weekly
yantra template lint weekly
yantra template save ./exec.md --name exec-brief --tags work
yantra template list --tag work
yantra template show weekly
yantra template remove weekly

yantra ask "summarize this week's delivery status" --template weekly
yantra research "compare the shortlisted vendors" --template tag:work
yantra do "prepare the launch review" --template ./launch.md
```

Library mutations affect `<data-dir>/templates/`. Template runs require a model;
combining `--template` with `--no-llm` or `LLM_PROVIDER=none` exits with a
validation error. See [Report Templates](features/report-templates.md).

## Profiles, preferences, history, and sites

Inspect the effective merged profile and manage SQLite-backed overrides:

```console
yantra profile
yantra prefs list
yantra prefs get defaults.detail
yantra prefs set defaults.detail full
yantra prefs set locale.city "Naperville, IL"
yantra prefs set context.location false
yantra prefs --forget defaults.detail
```

`profile` includes `profile.yaml`, built-in defaults, and stored overrides.
`prefs get` and `prefs list` show only SQLite rows. `--forget` deletes the
SQLite override and can reveal a still-present YAML value underneath it.

There is no separate `history` command. Use the run index and artifacts:

```console
yantra list
yantra list runs --limit 5 --status failed
yantra show <run-id> --events 50
yantra usage --type ask --since 7d
```

`usage --since` accepts `YYYY-MM-DD`, whole days such as `7d`, or whole hours
such as `24h`. The history index is a rebuildable cache; run directories remain
canonical. Read [Profiles and History](features/profiles-and-history.md).

Inspect or curate the local domain score table:

```console
yantra sites list
yantra sites add example.com
yantra sites remove example.com
```

`add` accepts an ASCII hostname, not a URL. Scores record search/fetch outcomes
but do not currently reorder research results or prove site trustworthiness.
See [Local Site Ranking](features/site-ranking.md).

## Scheduling and confirmations

Scheduling requires an existing lint-clean workflow. These commands modify the
local schedule store or daemon state:

```console
yantra schedule weekly-report --cron "0 9 * * 1" --params region=US
yantra schedules
yantra unschedule <schedule-id>

yantra daemon start
yantra daemon status
yantra daemon start --foreground
yantra daemon stop
```

Cron is interpreted in the machine's local timezone. The daemon polls every 60
seconds, skips missed startup occurrences, prevents overlap per schedule, and
does not install itself as an operating-system login service. Every scheduled
run is zero-LLM.

The only scheduling confirmation policy is `pause-and-notify`. Resolve a
genuinely parked run out of band:

```console
yantra confirm <run-id> grant
yantra confirm <run-id> deny
```

Granting records the decision; the daemon resumes the parked run on a later
poll. It never auto-confirms. See
[Scheduling and the Local Daemon](features/scheduling.md).

## Diagnostics and run inspection

```console
yantra doctor
yantra doctor --refresh --json
yantra audit <run-id>
yantra audit <run-id> --json
yantra report <run-id>
yantra report <run-id> --open
```

Ordinary `doctor` is offline. `doctor --agent-smoke` opens a live, potentially
billable provider session and requires credentials; it has no deterministic
fallback. `audit` reconstructs a trust narrative from stable Yantra-owned
projections and does not parse the raw provider session. `report` prints the
existing `report.md`; `--open` cannot be combined with `--json`. See
[Diagnostics and Audit](features/diagnostics-and-audit.md).

`init` creates local configuration or reports that it already exists:

```console
yantra init --provider none --yes
yantra init --reset
yantra init --json
```

`--reset` backs up and rewrites `config.yaml` and rewrites the profile. Use it
only when that replacement is intended.

## Shared flags and output behavior

`ask`, `research`, `do`, `run`, and `resume` register the same agent selection
surface:

| Flag                           | Purpose                                      | Default            |
| ------------------------------ | -------------------------------------------- | ------------------ |
| `--provider <name>`            | Model provider                               | `anthropic`        |
| `--model <id>`                 | Provider-scoped model id                     | `claude-haiku-4-5` |
| `--thinking <level>`           | Provider reasoning level                     | unset              |
| `--auth-secret <ref>`          | Runtime-only OS-keychain model-key reference | managed auth       |
| `--max-duration <duration>`    | Whole agent run deadline                     | `15m`              |
| `--max-tokens <n>`             | Cumulative provider-token ceiling            | `2000000`          |
| `--tool-timeout <duration>`    | One tool-attempt timeout                     | `3m`               |
| `--tool-retries <n>`           | Retries after an identical failure           | `3`                |
| `--confirm-timeout <duration>` | Live consent wait                            | `3m`               |
| `--no-llm`                     | Force a deterministic path where one exists  | model enabled      |

Durations are positive integers with optional `ms`, `s`, `m`, or `h`; a bare
integer is milliseconds. Resolution is command flag, then environment, then
effective `profile.yaml`/preference value, then pinned default. `--no-llm` or
`LLM_PROVIDER=none` is resolved before unused model settings.

`--json` is consistently machine-readable but not always the same stream shape:
inspection commands emit one JSON envelope, while live agentic commands can
emit newline-delimited progress and outcome records. `ask` and `research` also
support `--format terminal|md|html|json`, `--detail
overview|standard|full`, and `--length short|medium|long`; `--json` is shorthand
for JSON format. `--debug` writes diagnostics to stderr on commands that expose
it.

## Configuration basics

`config.yaml` controls shared runtime settings such as web-search selection.
`profile.yaml` contains personal defaults, context grants, and agent defaults.
Both are human-editable and live in the configuration directory shown in the
[quickstart](quickstart.md#initialize-local-configuration). Never store
credential values in either file.

A verified search block is:

```yaml
search:
  provider: auto
  fallback_chain:
    - tavily
    - brave
    - duckduckgo
  fetch_top: 3
```

`fetch_top` must be an integer from 1 through 5 and controls how many top search
hits agentic `web_search` fetches inline. It does not change deterministic
`ask --limit` behavior. Use `yantra profile` to see effective profile values and
their provenance, or `yantra prefs set` for a validated SQLite-backed override.

## Model and authentication basics

Agent option environment variables use the `YANTRA_AGENT_` prefix, including
`YANTRA_AGENT_PROVIDER`, `YANTRA_AGENT_MODEL`,
`YANTRA_AGENT_AUTH_SECRET`, `YANTRA_AGENT_MAX_DURATION`,
`YANTRA_AGENT_MAX_TOKENS`, `YANTRA_AGENT_TOOL_TIMEOUT`,
`YANTRA_AGENT_TOOL_RETRIES`, and `YANTRA_AGENT_CONFIRM_TIMEOUT`.

Credentials can come from Yantra-managed Pi auth, a runtime OS-keychain
reference, a supported provider environment variable such as
`ANTHROPIC_API_KEY`, or pinned custom-model configuration. Credential values
are not written to prompts or run artifacts. `ask`, `research`, and optional
replay synthesis can degrade deterministically when auth is absent; `do` and a
live doctor smoke fail with exit 3.

For Ollama, define the provider in `<data-dir>/pi/models.json`, select it with
`--provider ollama --model <id>`, and declare a `contextWindow` matching the
server. A 16k or larger context is recommended for web tasks. The local server
must already be running; Yantra does not start it.

## Artifacts and local state

Normal runs live under `<data-dir>/runs/<run-id>/`. Depending on the command,
the directory can include:

```text
manifest.json
events.jsonl
report.md
brief.json
brief.md
brief.html
document.json
document.md
document.html
outputs.json
checkpoints/
agent/*.jsonl
tool-calls.jsonl
usage.json
captures/
trace.json
schedule.json
```

The run directory is the durable observability record. The raw provider session,
captures, outputs, reports, and visited URLs can still contain sensitive data;
review the directory before sharing it. Browser profiles are ephemeral by
default. Saved workflows may explicitly use persistent workflow-scoped browser
profiles.

## Exit codes

|  Exit | Meaning                                                                                   |
| ----: | ----------------------------------------------------------------------------------------- |
|   `0` | Success; doctor warnings and partial deterministic results with notices can still succeed |
|   `1` | Invalid command input, option, YAML, template, or local lookup                            |
|   `2` | A started task failed, hit a fatal execution budget, or could not publish a valid result  |
|   `3` | Environment failure, failed doctor check, or unavailable required model setup             |
|   `4` | Human handoff, including CAPTCHA, MFA, missing required location, or denied consent       |
| `130` | An agentic run or live smoke was interrupted with Ctrl+C                                  |

## Current boundaries

- Yantra is a browser-automation and public-web research tool, not a general
  desktop, shell, filesystem, or code-execution agent.
- It honors robots policy, blocklists, rate limits, CAPTCHAs, and bot walls; no
  evasion flag exists.
- `do` requires a model. Deterministic operation is available through
  `ask --no-llm`, `research --no-llm`, and saved workflow replay.
- Search and model citations establish provenance, not correctness or freshness.
- Chrome state is ephemeral for agentic runs; persistent login state is not
  supported there.
- The local daemon does not start automatically at login or reboot.
- No public package or binary distribution is declared in the current private
  `0.0.0` manifests.

For a component-level view, see [Architecture](architecture.md). The remaining
feature references are [Ask](features/ask.md), [Research](features/research.md),
[Agentic Tasks](features/agentic-tasks.md),
[Saved Workflows](features/saved-workflows.md),
[Report Templates](features/report-templates.md),
[Profiles and History](features/profiles-and-history.md),
[Local Site Ranking](features/site-ranking.md),
[Scheduling](features/scheduling.md),
[Diagnostics and Audit](features/diagnostics-and-audit.md), and
[Safety and Privacy](features/safety-and-privacy.md).
